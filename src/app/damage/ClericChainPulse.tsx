import {useMemo,useState} from "react";
import type {TimedClericHealCall} from "./model";
import {buildChainPulse} from "./clericCadence";

const seconds=(value:number|null)=>value===null?"--":value.toFixed(1)+"s";
export function ClericChainPulse({calls,currentGap}:{calls:TimedClericHealCall[];currentGap:number}){
 const pulse=useMemo(()=>buildChainPulse(calls,currentGap),[calls,currentGap]);
 const[scaleInput,setScaleInput]=useState("15");
 const requestedScale=Number(scaleInput),max=Number.isFinite(requestedScale)&&requestedScale>0?Math.min(600,requestedScale):15,w=720,h=132,left=36,right=18,top=16,bottom=24,plotW=w-left-right,plotH=h-top-bottom;
 const count=Math.max(2,pulse.points.length+1),x=(index:number)=>left+index/(count-1)*plotW,y=(value:number)=>top+plotH-Math.min(max,value)/max*plotH;
 const line=pulse.points.map((point,index)=>`${x(index)},${y(point.gapSeconds)}`).join(" "),projectionX=x(pulse.points.length),last=pulse.points.at(-1),bandTop=pulse.medianSeconds===null?top:y(pulse.medianSeconds+pulse.toleranceSeconds),bandBottom=pulse.medianSeconds===null?top:y(Math.max(0,pulse.medianSeconds-pulse.toleranceSeconds));
 return <section className={`cleric-chain-pulse ${pulse.openGapTone}`}><header><div><span className="eyebrow">Chain pulse</span><h3>Cadence stability</h3><p>Recent gaps compared with the chain's learned median rhythm.</p></div><label className="chain-pulse-scale"><span>Scale 0 to</span><span className="chain-pulse-scale-input"><input aria-label="Cadence graph maximum seconds" type="number" min="1" max="600" step="1" inputMode="numeric" value={scaleInput} onChange={event=>setScaleInput(event.target.value)} onBlur={()=>setScaleInput(String(max))}/><b>s</b></span></label><div className="chain-pulse-metrics"><div><span>Stability</span><strong>{pulse.stabilityScore===null?"Learning":pulse.stabilityScore+"%"}</strong><small>{pulse.stabilityLabel}</small></div><div><span>Median</span><strong>{seconds(pulse.medianSeconds)}</strong><small>{pulse.points.length} gap{pulse.points.length===1?"":"s"}</small></div><div><span>Trend</span><strong>{pulse.trend}</strong><small>{pulse.deviationSeconds===null?"Waiting for rhythm":"± "+pulse.deviationSeconds.toFixed(1)+"s variation"}</small></div><div className="open"><span>Open gap</span><strong>{currentGap.toFixed(1)}s</strong><small>{pulse.openGapTone==="overdue"?"Past the learned window":pulse.openGapTone==="due"?"Next call is due":"Building toward next call"}</small></div></div></header><figure><svg data-scale-max={max} viewBox={`0 0 ${w} ${h}`} role="img" aria-label={`Complete Heal cadence chart. ${pulse.stabilityLabel}, median ${seconds(pulse.medianSeconds)}, current gap ${currentGap.toFixed(1)} seconds.`}><line className="axis" x1={left} x2={w-right} y1={y(0)} y2={y(0)}/>{[0,.5,1].map(scale=><g key={scale}><line className="grid" x1={left} x2={w-right} y1={y(max*scale)} y2={y(max*scale)}/><text x={left-6} y={y(max*scale)+3} textAnchor="end">{Math.round(max*scale)}s</text></g>)}{pulse.medianSeconds!==null&&<><rect className="pace-band" x={left} y={bandTop} width={plotW} height={Math.max(2,bandBottom-bandTop)}/><line className="median" x1={left} x2={w-right} y1={y(pulse.medianSeconds)} y2={y(pulse.medianSeconds)}/></>}{pulse.points.length>1&&<polyline className="cadence-line" points={line}/>} {pulse.points.map((point,index)=><circle key={`${point.callNumber}-${index}`} className={point.tone} cx={x(index)} cy={y(point.gapSeconds)} r="5"><title>{`Call #${String(point.callNumber).padStart(3,"0")}: ${point.gapSeconds.toFixed(1)}s - ${point.tone}`}</title></circle>)}{last&&<line className={`projection ${pulse.openGapTone}`} x1={x(pulse.points.length-1)} y1={y(last.gapSeconds)} x2={projectionX} y2={y(currentGap)}/>}<circle className={`now ${pulse.openGapTone}`} cx={projectionX} cy={y(currentGap)} r="6"><title>{`Current open gap: ${currentGap.toFixed(1)}s`}</title></circle><text className="now-label" x={projectionX} y={Math.max(10,y(currentGap)-10)} textAnchor="middle">NOW</text></svg><figcaption><span><i className="stable"/>Within rhythm</span><span><i className="early"/>Early</span><span><i className="late"/>Late</span><span><i className="now"/>Current gap</span></figcaption></figure></section>;
}